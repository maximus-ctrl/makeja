# Common Errors

## 401 unauthenticated
Login again and confirm the frontend stores the API token.

## 403 feature disabled
Check backend `.env` and `config/features.php`.

## M-Pesa timeout
Confirm credentials, shortcode, passkey, phone format, and callback URL.
